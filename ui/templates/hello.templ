package templates

import "firecrest-go/tutorial"

templ Home(events []tutorial.Event) {
	@Html("Home", nil) {
		<h1>Welcome to the Home Page</h1>
		<div>
			for _, event := range events {
				<a href={ templ.URL("/events/" + event.Slug) }>{ event.Name }</a>
			}
		</div>
	}
}

templ Event(author tutorial.Event) {
	@Html("Event Details", nil) {
		<h1>{ author.Name }</h1>
		<h1>{ author.Slug }</h1>
	}
}

templ MetaTags(description, keywords string) {
	<meta name="description" content={ description }/>
	<meta name="keywords" content={ keywords }/>
}

templ ServerError() {
	@Html("Server Error", nil) {
		<h1>500 - Internal Server Error</h1>
		<p>Sorry, something went wrong on our end.</p>
	}
}
